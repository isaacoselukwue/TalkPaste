# Development + CI dependencies. Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest>=7.4
pytest-cov>=4.1
pytest-qt>=4.3            # exercised only when PySide6 is installed
ruff>=0.4                 # lint + format
mypy>=1.8                # static type checking
pyinstaller>=6.3         # packaging
