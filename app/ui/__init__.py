"""PySide6 user interface: tray app, settings window and status popup.

Importing this package does not import PySide6 at module load; the submodules
import Qt lazily so the CLI can run in environments without Qt installed.
"""
